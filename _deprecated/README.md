# _deprecated/

Files kept for history, not part of ReadyTrader-Crypto. Nothing here is loaded, tested or documented
as current.

- `configs/agent_zero.mcp.yaml`: an `mcp_servers:` YAML block for an `agent.yaml` that current Agent Zero
  (v2.13) does not read for MCP servers. Replaced by `configs/agent_zero.mcp.json`, the JSON that goes in
  Agent Zero's Settings → MCP/A2A → External MCP Servers (see the README's Integration Guide).
