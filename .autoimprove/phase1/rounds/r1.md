# Round 1

## Hypothesis

Train `crash-05` fails because it has only 3 lexicon hits (`dumped`, `dumping`, `bleeding`) — one short of `MIN_DIRECTIONAL=4` and of the 15-text share floor (3.75). Several remaining train lines only print a large drop (`$BTC -10%`, `(-9.8% 24h)`) without crash/panic words.

One rule: treat a printed 8%+ drop as bearish (minus must sit on the number), and when at least one such print is present, accept 3 directional texts as consensus.

Justified from train only. Held-out `crash-02` was not used to pick words.

Feeds `crash-pct-only`, `red_day-six-pct`, `calm-promo-pct` were added first and start held-out. They do not change the frozen primary-metric denominator.

## Train (scored first)

| id | before | after | blocks |
| -- | ------ | ----- | ------ |
| crash-01 | -1.0 / 4 bear | -1.0 / 9 bear | yes |
| crash-07 | -1.0 / 8 bear | -1.0 / 9 bear | yes |
| crash-exchange-hack | -1.0 / 6 bear | -1.0 / 6 bear | yes |
| crash-05 | 0.0 / 3 bear, known_limit | **-1.0 / 5 bear** | **yes** (flag removed after true score change) |
| calm-01, red_day-01, green_day-03, contested-01, alarming-jokes | no block | no block | no |

Train crash blocks: 3/4 → **4/4**. Train improved; score held-out.

## Held-out (frozen ids)

| id | before | after | blocks |
| -- | ------ | ----- | ------ |
| crash-03 | -1.0 | -1.0 / 8 bear | yes |
| crash-12 | -1.0 | -1.0 / 7 bear | yes |
| crash-02 | 0.0 / 1 bear, known_limit | **-1.0 / 3 bear** | **yes** (flag removed after true score change) |
| calm-02, red_day-02, contested-05, alarming-anniversary | no new block | no new block | no |
| alarming-altcoin-implodes | known_limit block | still known_limit block | yes (allowed) |

Held-out crash-block recall: 2/3 → **3/3** (+1 crash block).

## Extra held-out feeds

| id | after | notes |
| -- | ----- | ----- |
| crash-pct-only | -1.0 / 8 bear, blocks | expected |
| red_day-six-pct | 0.0, no block | 5.8% < 8% |
| calm-promo-pct | 0.0, no block | ` - 92% WIN RATE` ignored |

## Guards

| Guard | Result |
| ----- | ------ |
| 0 held-out calm / red_day / green_day / contested blocks | pass |
| no new alarming FPs (altcoin stays known_limit) | pass |
| paper/safety pack (same 12 files) | 284 passed / 0 failed (278 + 3 feeds + 3 tests), 24.592s |
| degraded-refresh tighten-only | pass |
| `sentiment.status` contract | pass |
| `test_risk.py` Falling Knife strings | unchanged |
| live_safety_gates + paper_order_integrity | green |
| `examples/paper_btc_uat.py` | pass: true |
| Flags | PAPER_MODE=true LIVE_TRADING_ENABLED=false TRADING_HALTED=true |

## Verdict

**ACCEPT.** Held-out crash-block recall 2/3 → 3/3 and every guard held. `known_limit` removed on `crash-05` / `crash-02` only after scores crossed `-0.5`.
